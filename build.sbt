name := "my-scala-project"

version := "0.1"

scalaVersion := "2.13.8"  // Or any appropriate version

// This is optional, but ensures SBT knows where to find the main class
mainClass in Compile := Some("Main")
